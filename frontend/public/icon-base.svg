<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Fondo rosa pastel -->
  <defs>
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#FFB6C1;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#FFC0CB;stop-opacity:1" />
    </linearGradient>
  </defs>
  
  <!-- Fondo -->
  <rect width="512" height="512" fill="url(#bgGradient)" rx="64"/>
  
  <!-- Círculo decorativo -->
  <circle cx="256" cy="256" r="180" fill="rgba(255,255,255,0.3)"/>
  
  <!-- Texto OsitosLua (iniciales) -->
  <text x="256" y="320" font-family="Arial, sans-serif" font-size="200" font-weight="bold" 
        fill="#8B4513" text-anchor="middle" dominant-baseline="middle">
    OL
  </text>
  
  <!-- Emoji osito (puede no renderizar en todos los navegadores) -->
  <!-- <text x="256" y="280" font-size="250" text-anchor="middle">🧸</text> -->
</svg>
