{
  "name": "ositoslua-monorepo",
  "version": "1.0.0",
  "description": "OsitosLua E-commerce Platform - Scripts de desarrollo",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,magenta\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ecommerce", "react", "nodejs", "postgresql"],
  "author": "Luis Reyes",
  "license": "ISC"
}
